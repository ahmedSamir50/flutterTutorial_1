import 'package:flutter/material.dart';

class ShoppingAppLayout extends StatelessWidget {
  const ShoppingAppLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
